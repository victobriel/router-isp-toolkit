/** Default max wait after click (ms). Increased for slow/overloaded routers. */
export const DEFAULT_MAX_WAIT_AFTER_CLICK_MS = 10000;

/** Default max wait for disappearance (ms). */
export const DEFAULT_MAX_WAIT_AFTER_DISAPPEARANCE_MS = 10000;

/** Default max wait for element (ms). */
export const DEFAULT_MAX_WAIT_AFTER_ELEMENT_MS = 10000;

/** Default max wait for input populated (ms). */
export const DEFAULT_MAX_WAIT_AFTER_INPUT_POPULATED_MS = 10000;
