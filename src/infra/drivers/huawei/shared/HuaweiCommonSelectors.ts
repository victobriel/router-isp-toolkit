/**
 * Common CSS selectors shared between Huawei router admin UIs.
 *
 * If future models diverge, update the per-model selectors modules by spreading
 * this base and overriding only the fields that differ.
 */
export const HuaweiCommonSelectors = {} as const;
