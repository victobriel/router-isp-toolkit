/** Max wait for DHCP server container to appear (ms). */
export const DHCP_LAN_ALLOCATED_ADDRESS_MAX_WAIT_MS = 20000;

/** Max wait for topology popup data rows to load (ms). */
export const TOPOLOGY_CLIENTS_LOAD_MAX_WAIT_MS = 20000;

/** Settle time before considering topology popup content ready (ms). */
export const TOPOLOGY_POPUP_SETTLE_MS = 2500;

/** Default max wait after click (ms). */
export const DEFAULT_MAX_WAIT_AFTER_CLICK_MS = 5000;

/** Default max wait for disappearance (ms). */
export const DEFAULT_MAX_WAIT_AFTER_DISAPPEARANCE_MS = 10000;
