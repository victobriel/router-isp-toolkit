/**
 * Common driver wait-time constants shared by ZTE ZXHN H199 / H3601.
 *
 * If future models diverge, consider adding per-model overrides while keeping
 * the shared subset here.
 */
export const DHCP_LAN_ALLOCATED_ADDRESS_MAX_WAIT_MS = 20000;
export const TOPOLOGY_CLIENTS_LOAD_MAX_WAIT_MS = 20000;
export const TOPOLOGY_POPUP_SETTLE_MS = 2500;
